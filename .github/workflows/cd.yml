name: CD Workflow
on:
  workflow_dispatch:
    inputs:
        ARTIFACT_NAME:
            description: 'Artifact name'
            required: true
            type: string
run-name: "CD-[${{inputs.ARTIFACT_NAME}}]-[${{ github.ref_name }}]"            
jobs:
  deploy:
    uses: the-palace-company/workflows-serverless-deployments/.github/workflows/cd.yml@main
    with:
      ARTIFACT_NAME: ${{inputs.ARTIFACT_NAME}}
    secrets: inherit
